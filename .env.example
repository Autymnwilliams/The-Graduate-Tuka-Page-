# Mapbox GL JS public access token (https://account.mapbox.com/access-tokens/).
# Without this set, the map section falls back to a simple pin-list UI.
NEXT_PUBLIC_MAPBOX_TOKEN=

# MongoDB connection string, for reviews and likes.
# A free MongoDB Atlas (M0) cluster is plenty for a pilot: create one at
# https://www.mongodb.com/cloud/atlas, then paste its connection string here.
# Without this set, reviews/likes fall back to in-memory storage that resets
# on every restart — fine for local dev, not production.
MONGODB_URI=

# Google Places API (New) key, for real business photos when a rec has no
# manually-supplied photoUrls. Requires a Google Cloud project with billing
# enabled and the "Places API (New)" enabled — see
# https://console.cloud.google.com/google/maps-apis. Never exposed to the
# client: photos are proxied server-side through /api/rec-photo. Without
# this set, recs with no manual photos just show a category-icon fallback.
GOOGLE_PLACES_API_KEY=

# Gemini API key, for the digital concierge chat (/api/chat) scoped to each
# hotel's own curated recs. Get one at https://aistudio.google.com/apikey.
# Without this set, the chat widget returns a 503 and stays unusable.
GEMINI_API_KEY=

# Uber developer client_id, for the "Get an Uber there" deep link
# (https://developer.uber.com/docs/deeplinks — no approval process needed
# for this pre-filled-destination flow, just create an app in the Uber
# developer dashboard). Without this set, the Uber button doesn't render.
UBER_CLIENT_ID=

# Twilio credentials, for the concierge SMS sent when a guest likes/favorites
# a rec (requires the guest to have also signed up with a phone number).
# Same env var names as the backend_main Express app for consistency.
# Without these set, SMS sends are skipped (logged, not fatal).
TWILIO_ACCOUNT_SID=
TWILIO_API_KEY_SID=
TWILIO_API_KEY_SECRET=
TWILIO_PHONE_NUMBER=

# Twilio's classic Auth Token (Console > Account > API keys & tokens) --
# different from the API Key SID/Secret above, only used to verify inbound
# webhook signatures (app/api/sms/inbound). Without this set, the inbound
# STOP/START/HELP webhook still works but skips signature verification,
# which isn't safe for production.
TWILIO_AUTH_TOKEN=


# Real availability calendar (lib/resy.ts) — off by default. Resy doesn't
# publish an official API; this calls the same endpoints resy.com's own
# browser JS calls, using only their PUBLIC (non-secret) web-app key, no
# login required. Still an unofficial/undocumented endpoint their ToS
# technically covers for automated use, so this stays a conscious opt-in.
# Set to "true" to enable; the availability calendar always falls back to a
# simulated one per-day regardless (no match, request failure, or flag off).
ENABLE_REAL_AVAILABILITY=

# Override Resy's public web-app API key if they ever rotate it. Leave blank
# to use the known-working default baked into lib/resy.ts.
RESY_API_KEY=

# Cloudinary credentials, for staff-uploaded rec photos (staff page ->
# Manage Recommendations -> Photos). Same account backend_main already
# uses -- copy these three values directly from that app's existing
# config (backend_main/.env or Cloudinary dashboard). Without these set,
# photo uploads fail (existing Google Places / icon fallback still works).
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Clerk, for real hotel staff/guest accounts. MUST be the exact same
# Clerk application backend_main and circle-frontend-web already use
# (same keys, copied directly from backend_main/.env) -- that's what
# makes a signed-in session automatically shared across tuka.world and
# app.tuka.world, since Clerk shares sessions across subdomains of one
# root domain by default, no extra config needed.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
